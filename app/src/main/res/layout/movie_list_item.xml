<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/image_movie_poster"
        android:adjustViewBounds="true"
        android:scaleType="centerCrop"
        />

    <!--This TextView holds the movie title -->
    <TextView
        android:id="@+id/tv_movie_title"
        style="@style/TextAppearance.AppCompat.Medium"
        android:textColor="@android:color/white"
        android:textStyle="bold"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/image_movie_poster"
        android:layout_alignLeft="@id/image_movie_poster"
        android:layout_marginLeft="16dp"
        android:layout_marginTop="16dp"        />

    <!--This TextView holds the movie vote average -->
    <TextView
        android:id="@+id/tv_movie_vote_avg"
        android:textColor="@android:color/white"
        android:textStyle="bold"
        android:textSize="16sp"
        android:background="@drawable/vote_avg_circle"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:gravity="center"
        android:layout_alignTop="@id/image_movie_poster"
        android:layout_alignRight="@id/image_movie_poster"
        android:layout_marginRight="16dp"
        android:layout_marginTop="16dp"
        />

    <!--This View serves as a visual divider between list items-->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#dadada"
        android:layout_marginLeft="8dp"
        android:layout_marginRight="8dp" />

</RelativeLayout>